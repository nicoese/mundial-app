DB_USER=postgres
DB_PASSWORD=root
DB_HOST=localhost:5432
DB_NAME=world-cup

estas variables de entorno son las que hacen falta para que se conecte la db con la api
agregar un archivo .env en el directorio raiz de la api

CREAR BASE DE DATOS EN POSTGRES. SE TIENE QUE LLAMAR 'world-cup'